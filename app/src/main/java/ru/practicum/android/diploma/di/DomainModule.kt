package ru.practicum.android.diploma.di

import org.koin.dsl.module

val domainModule = module {
    // Здесь будут определены зависимости для domain слоя
    // Например: Use Cases (Interactors), Repository интерфейсы и т.д.
}

